requests>=2.31
cryptography>=42.0
PyYAML>=6.0
colorama>=0.4.6
playwright>=1.40  # optional - only needed for spot_lean's CF Benchmarks scraping source; run 'playwright install chromium' once after pip install
flask>=3.0        # only needed for the webapp/ dashboard (server.py)
ruamel.yaml>=0.18  # only needed for the webapp/ dashboard - preserves config.yaml's comments when saving from the UI
